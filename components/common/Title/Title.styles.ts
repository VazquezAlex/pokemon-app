// Core imports.
import { StyleSheet } from "react-native";

export const titleStyles = StyleSheet.create({

    title: {
        color: 'white',
        fontSize: 48,
        fontWeight: '700',
    }

});